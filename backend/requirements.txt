fastapi>=0.110
uvicorn[standard]>=0.29
pydantic-settings>=2.2
httpx>=0.27
websockets>=12  # screenshot-to-code /generate-code protocol
playwright>=1.54  # OpenEngine browser actuation
pillow>=10  # optical perception preprocessing + set-of-marks annotation
# Optional engines — install per station:
#   nova-act          (journey execution)
#   camel-oasis       (social mirror runtime)
